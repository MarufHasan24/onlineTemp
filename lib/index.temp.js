(function () {
  /*
      Title: index.js
      Author: Maruf Hasan
      Description: lib for client side req to do and resove stuff fo wikipedia
      Date: 08 August, 2024
      */

  function wordAdded(title, fromdate, callback, todate) {
    //check every revisions and count their words from changes
  }
  function totalWordCount(title, callback) {
    //just count total word of the page
  }
  function tableMaker(data, callback) {
    //create html table from json
  }
  function messenger(title, msg, callback, place = 0) {
    //add the msg to that page
  }
})();
